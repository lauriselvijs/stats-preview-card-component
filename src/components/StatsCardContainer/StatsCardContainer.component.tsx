import React from "react";
import { IContainer } from "../../types/Container";

const StatsCardContainer = ({ children }: IContainer) => {
  return <main>{children}</main>;
};

export default StatsCardContainer;
